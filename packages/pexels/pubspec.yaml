name: pexels
description: A comprehensive Dart client for the Pexels API. Search and access photos, videos, and collections.
version: 1.0.0

publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  http: ^1.6.0
  json_annotation: ^4.11.0

dev_dependencies:
  build_runner: ^2.12.2
  json_serializable: ^6.13.0
  lints: ^6.1.0
  test: ^1.30.0
